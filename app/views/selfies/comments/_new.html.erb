<%= form_for [ @selfie, Comment.new ], remote: true do |f| %>

  <%= f.text_area :body,  class: "form-control" %>
  <%= button_tag( :class => "form_btn") do %>
    <i class="material-icons">send</i>
  <% end %>


<% end %>
